# Smart Ticket Routing Service
A backend microservice responsible for ingesting, validating, and prioritizing support tickets via webhooks. Built to handle high-throughput event streams from platforms like Jira and GitHub.

## Installation
1. Clone the repo
2. `npm install`
3. Create a `.env` file with `MONGO_URI`
4. `npm start`